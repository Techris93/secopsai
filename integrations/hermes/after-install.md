# SecOpsAI for Hermes Agent

The read-only SecOpsAI tools are installed. Complete Core onboarding and the background monitor with:

```bash
curl -fsSL https://secopsai.dev/install-hermes.sh | bash
```

Verify the integration with `secopsai hermes doctor` and `hermes plugins list`.

